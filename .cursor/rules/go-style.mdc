---
description: Go 코딩 스타일 가이드
globs: "**/*.go"
alwaysApply: false
---

# Go 코딩 스타일

- 외부 패키지를 절대 import하지 않는다. 표준 라이브러리만 사용한다.
- 에러는 반드시 처리한다. `_`로 무시하지 않는다.

```go
// BAD
result, _ := doSomething()

// GOOD
result, err := doSomething()
if err != nil {
    return fmt.Errorf("doSomething failed: %w", err)
}
```

- `os.Exit`은 `main.go`에서만 호출한다. `cmd/` 패키지 함수는 error를 반환한다.
- 함수명은 역할을 명확히 드러낸다: `Recommend`, `Wiki`, `Help` 등
- `fmt.Fprintf(os.Stderr, ...)` 로 에러 메시지를 stderr에 출력한다.
